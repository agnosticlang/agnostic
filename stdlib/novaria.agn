package novaria

pub fn Exit(code int) {
    return
}

// read-only; no create/write syscall exists for disk files
pub fn Open(filename string) int {
    return -1
}

pub fn Read(fd int, bufferOffset int) int {
    return -1
}

pub fn Write(fd int, bufferOffset int) int {
    return -1
}

pub fn Remove(filename string) {
    return
}

pub fn Exec(filename string) {
    return
}

pub fn MemAlloc(size int) int {
    return 0
}
