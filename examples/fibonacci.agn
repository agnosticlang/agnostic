package main

import "stdio"

func fib(n int) int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

func main() {
    var i int = 0
    for i < 15 {
        stdio.Println(fib(i))
        i = i + 1
    }
}
